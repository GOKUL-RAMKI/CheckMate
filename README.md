# CheckMate
CheckMate is a C-based manufacturing QC system managing components from production to inspection. Components start as PENDING, get APPROVED if all parameters pass, or REJECTED on failure. Rejected items can be fixed and reset to PENDING for reinspection, simulating a real industrial workflow.
